server:
  port: 10000

spring:
 application:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: a
    password: a
  h2:
    console:
      enabled: true
  jpa:
    databasePlatform: org.hibernate.dialect.H2Dialect
      
management:
  endpoints:
    web:
      exposure:
        include: '*'
        
