server:
  port: 10000

      
management:
  endpoints:
    web:
      exposure:
        include: '*'
        
eureka:
 client:
   serviceUrl:
     defaultZone: http://localhost:8761/eureka
     

app:
 topic:
  pending-orders: pending.orders.t


